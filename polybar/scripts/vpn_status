#!/bin/bash

# Проверяем, активен ли туннельный интерфейс (замените tun0 на ваш интерфейс)
if ip a | grep -q "amn0"; then
    echo "VPN: "  # Иконка замка для активного VPN
else
    echo "VPN: "  # Иконка разомкнутого замка для неактивного VPN
fi